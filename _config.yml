title: DB Conference Deadlines
description: Countdowns to top Database/Data Mining/Information Retrieval/ML/Theory conference deadlines
author: merfanian

domain: "merfanian.github.io"
baseurl: "/db-deadlines"
url: "https://merfanian.github.io"

twitter_hashtag: db_deadlines
github_username: merfanian
github_repo: db-deadlines

markdown: kramdown
ga_id:

include: ['_pages', 'api']

# API: expose JSON endpoints for agent/tool consumption
# Built by _plugins/api_json_generator.rb at build time:
#   /conferences.json          — all conferences
#   /api/{SUB}.json            — per-subject filtered lists
#   /api/upcoming.json         — non-TBA deadlines
#   /api/index.json            — endpoint directory
